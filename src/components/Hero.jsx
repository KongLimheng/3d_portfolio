import { motion } from 'framer-motion';
import { styles } from '../styles';

const Hero = () => {
  return (
    <section className="relative w-full h-screen mx-auto">
      Hero s<div className={`${styles.paddingX} absolute`}>hi</div>
    </section>
  );
};

export default Hero;
